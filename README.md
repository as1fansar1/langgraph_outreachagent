# langgraph_outreachagent

Outreach agent built with LangGraph + LangChain + Streamlit. Researches a prospect from a name and company, then drafts personalized cold outreach grounded in what it found.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Env
- `OPENAI_API_KEY` (required)
